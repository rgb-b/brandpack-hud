# Backlog & Known Issues

Open bugs and feature requests. Remove items when resolved.

---

## Bugs

- **Weekly maintenance reminder** — "Mark complete" button has no function. Should open the maintenance log tool and allow user to add a report. Maintenance log should have filterable categories: routine weekly maintenance vs service visits (e.g. technician called for a scheduled service) should both appear in the log but be distinguishable by filter/category.

- **Dashboard "Current Task" widget** — Large blank space below the counter. Widget box expands with the to-do list length, making the issue worse with longer to-do lists.

- **"Today's Time" stat** — Only showing the last tracked total, not the cumulative total for the full day.

---

## Feature Requests

- **Pantone Connect integration** — Check if the Pantone tool can connect to Pantone Connect (premium account available). Would enable live colour data sync.

- **Productivity auto-detection** — Investigate whether the productivity tracker can detect when the user is at/away from their computer, or pull data from other apps/phone to automatically determine activity.
